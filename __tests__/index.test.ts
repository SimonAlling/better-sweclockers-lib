import {
  insert,
  placeCursorIn,
  replaceSelectionWith,
  selectedTextIn,
  selectRangeIn,
  wrap_tag,
  wrap_verbatim
} from "../src/index";

it("exports what it should", () => {
  // This code wouldn't compile if it didn't.
});
